{{!< chat}}

<div class="registration">
    <div class="registration__wrapper">
        <h1 class="registration__title">Регистрация</h1>
        {{> "inputs/input-firstname"}}
        {{> "inputs/input-secondname"}}
        {{> "inputs/input-login"}}
        {{> "inputs/input-email"}}
        {{> "inputs/input-password"}}
        {{> "inputs/input-phone"}}
        <a href="general.hbs"><button class="registration__sign-up btn">Зарегистрироваться</button></a>
    </div>
</div>